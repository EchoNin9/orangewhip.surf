/**
 * config.js — Runtime configuration for Orange Whip Surf SPA
 *
 * This file is loaded before the React app starts and provides
 * environment-specific configuration values.
 *
 * For local development, these values can be set to mock/test values.
 * In production, this file is generated by the deployment pipeline.
 */

// Cognito configuration
// For local development without a real Cognito pool, you can leave these empty
// The app will gracefully handle missing auth configuration
window.COGNITO_USER_POOL_ID = '';
window.COGNITO_CLIENT_ID = '';

// API Gateway endpoint
// For local development, point to a local mock API or use a placeholder
// The app will handle API errors gracefully if the backend is not available
window.API_BASE_URL = 'http://localhost:8000';

// Media bucket URL (for presigned uploads)
window.MEDIA_BUCKET_URL = '';

// Environment flag
window.APP_ENV = 'development';
